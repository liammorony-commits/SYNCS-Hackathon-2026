{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start Expo (Run Task)",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}",
      "preLaunchTask": "start expo",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
